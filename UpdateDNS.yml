# ansible-playbook 

hosts: "*"
become: true
tasks: 
   - name: upgrade the cloud dns servers
      apt: 
       upgrade: dist
       full_upgrade: yes
      autoremove: yes
 register: upgrade_output     
     
  - debug: 
        var: upgrade_output.stdout_lines
          
